"""ATP core — domain, strategy, risk, backtest and analytics.

Pure Python: no network, no filesystem, no wall-clock reads. Everything external
sits behind a Protocol in a `ports.py` module (CLAUDE.md §1.3).
"""

__version__ = "0.1.0"
